import { createSlice } from '@reduxjs/toolkit'

const notificationSlice = createSlice({
    name: 'notification',
    initialState: 'Here be notifications',
    reducers: {

    },
})

export default notificationSlice.reducer